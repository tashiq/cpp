#include<bits/stdc++.h>
using namespace std;

    int main(){

    srand(time(0));
    int n = rand() + 200;
    vector<double> v(n);
    for(int i =0; i<n; i++){
        double d = rand()/float(RAND_MAX);
        v[i]=d;
    }

    vector<double> choose(5);
        choose[j] = v[in];
        cout<<choose[j]<<" ";
    }
    for(int j=0; j<5; j++){
        int in = rand()%n;

}


